version: "1.0"
buildspecs:
  install_specs_example:
    type: spack
    executor: generic.local.bash
    description: "Install zlib spec"
    tags: [spack]
    spack:
      root: $HOME/spack
      install:
        specs: ['zlib']