modules:
  default:
    enable:
      - tcl
    roots:
      tcl: /home/spack/modules
    tcl:
      # Keep a hash in module names so distinct concretizations do not collide.
      hash_length: 7
      projections:
        all: '{name}/{version}-{compiler.name}-{compiler.version}'
