stages:
  - test
pr_cori_test:
  tags:
    - cori-login
  stage: test
  only:
    - external_pull_requests
  script:
    - pwd && ls -l
    - hostname
    - cd buildtest
    - git rev-parse HEAD
    - source setup.sh
    - buildtest --version
