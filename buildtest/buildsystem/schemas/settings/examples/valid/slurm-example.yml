executors:
  slurm:
    haswell:
      options: ["-C haswell"]
      launcher: sbatch
      pollinterval: 20
      modules:
        purge: True
        load: [PrgEnv-intel]
config:
  editor: vi
  paths:
    prefix: /tmp