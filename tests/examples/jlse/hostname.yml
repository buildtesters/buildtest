version: "1.0"
buildspecs:
  iris_hostname:
    executor: jlse.cobalt.iris
    type: script
    tags: [jobs]
    description: Run hostname on iris queue
    cobalt: ["-n 1", "-t 10"]
    run: hostname