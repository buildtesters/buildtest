editor: vi
executors:
  normal:
    type: slurm
    description: submit jobs with slurm using srun instead of sbatch
    launcher: sbatch
    vars:
      FOO: BAR
    options:
      - "-p normal"
      - "-t 00:10"
      - "-N 1"
      - "-n 1"
