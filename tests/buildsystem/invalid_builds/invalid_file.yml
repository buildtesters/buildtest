version: "1.0"
buildspecs:
  invalid_file:
    type: compiler
    executor: local.bash
    description: Can't find source file name
    source: /path/to/invalidfile
    compilers:
      name: ["gcc"]
      default:
        gcc:
          cflags: -O3

