buildspecs:
  symlink_test:
   type: script
   executor: generic.local.bash
   description: status check based on symbolic link
   run: |
      ln -s /tmp scratch
   status:
     is_symlink:
       - scratch