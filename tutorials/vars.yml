version: "1.0"
buildspecs:
  variables:
    type: script
    executor: local.bash
    description: Declare shell variables
    tags: [tutorials]
    vars:
      X: 1
      Y: 2
    run: echo "$X+$Y=" $(($X+$Y))
    
